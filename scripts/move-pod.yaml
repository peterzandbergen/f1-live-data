apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
  namespace: default
spec:
  nodeSelector:
    kubernetes.io/hostname: istio-cluster-worker3
  containers:
  - image: nginx
    imagePullPolicy: Always
    name: nginx
